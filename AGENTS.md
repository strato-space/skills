# AGENTS

## Repository scope
- This repo stores reusable skills and packaged `.skill` artifacts.
- Keep documentation in English (`README.md`, `CHANGELOG.md`, `docs/*`).

## Update expectations
- When adding or changing skills, update `CHANGELOG.md` and relevant docs.
- Keep skill folders self-contained with a `SKILL.md` at the root of each skill.
